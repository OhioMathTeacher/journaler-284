# Throwaway pages used to drive the app in a headless browser while testing.
# One of these (_probe.html) got committed in 045694f; this is so it cannot happen again.
_probe.html
_p_*.html
_q_*.html
# The probe chapter tools/probes/run.py builds and then deletes.
_p_*.pdf

# Python bytecode. Was tools/__pycache__/ alone, which is why a root __pycache__/ and
# tools/probes/__pycache__/ both turned up untracked after a probe run. A bare name
# matches at every depth.
__pycache__/
